VITE_GOST_API_BASE=http://your-host:18080/api
VITE_GOST_USER=admin
VITE_GOST_PASS=changeme

# Optional — only if you deployed the gost-logfeed sidecar with a TOKEN.
# Leave blank for an unauthenticated sidecar (only acceptable on localhost / a
# private overlay where you trust everyone).
VITE_GOST_LOGFEED_TOKEN=
